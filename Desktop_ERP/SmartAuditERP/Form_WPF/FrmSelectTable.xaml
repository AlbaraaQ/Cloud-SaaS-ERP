<dx:ThemedWindow
    x:Class="SmartAuditERP.Form_WPF.FrmSelectTable"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    Title="اختيار الطاولات"
    Width="960" Height="520"
    WindowStartupLocation="CenterScreen"
    Background="#1E1E2E"
    FontFamily="Cairo, Tahoma, Arial"
    WindowStyle="None"
    AllowsTransparency="True"
    Loaded="Window_Loaded">

    <dx:ThemedWindow.Resources>
        <Style x:Key="DeptBtn" TargetType="Button">
            <Setter Property="Width" Value="110"/>
            <Setter Property="Height" Value="75"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#0FBC0F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextBlock.TextAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="#1B9CFC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </dx:ThemedWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="14"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="15"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4"
                Background="#2F2B5B" Padding="8,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="btnClose" Grid.Column="0"
                        Content="✕" Width="42" Height="36"
                        Background="#C00000" Foreground="White"
                        FontSize="14" FontWeight="Bold"
                        BorderThickness="0" Cursor="Hand"
                        Click="btnClose_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <TextBlock Grid.Column="1"
                           Text="🍽️ اختيار الطاولات"
                           Foreground="White" FontSize="15" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <Button x:Name="btnAddGrp" Grid.Column="2"
                        Content="➕ إضافة مجموعة"
                        Background="#4B3D8F" Foreground="White"
                        FontSize="11" FontWeight="Bold"
                        BorderThickness="0" Cursor="Hand"
                        Padding="10,6" Height="32"
                        Click="btnAddGrp_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="6"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <!-- شريط جانبي الأقسام -->
        <Border Grid.Row="1" Grid.Column="0" Background="#2F2B5B"/>
        <Border Grid.Row="1" Grid.Column="3" Background="#2F2B5B"/>

        <!-- منطقة الطاولات -->
        <ScrollViewer Grid.Row="1" Grid.Column="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto">
            <WrapPanel x:Name="FlowLayoutPanel1"
                       Orientation="Horizontal"
                       Margin="4"/>
        </ScrollViewer>

        <!-- الأقسام -->
        <ScrollViewer Grid.Row="1" Grid.Column="2"
                      VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="FlowLayoutPanelDept"
                       Orientation="Horizontal"
                       Margin="4"/>
        </ScrollViewer>

        <!-- شريط سفلي -->
        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4"
                Background="#2F2B5B"/>
    </Grid>
</dx:ThemedWindow>