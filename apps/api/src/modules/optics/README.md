# Optics pack (Phase 22)

Feature flag: `pack.optics`. Stores party/invoice-line prescriptions with typed JSON for right/left eye (`sph/cyl/axis/add/ipd`) and the legacy `Other_Column` R/L grid. The invoice print endpoint returns a conditional prescription section.
